from utils.config import DB_URL
from pymongo import MongoClient

# question from which date to fetch the articles so the articles are latest
# question what to do with the old articles
# question when to run the code 

